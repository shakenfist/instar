[package]
name = "commit"
version = "0.2.0"
edition = "2021"
description = "Plan in-place metadata mutations to commit overlay clusters into a backing image (no I/O)"
license = "Apache-2.0"
publish = false

[dependencies]
shared = { path = "../../shared" }
qcow2  = { path = "../qcow2", features = ["create"], default-features = false }
vmdk   = { path = "../vmdk", default-features = false }

[dev-dependencies]
create = { path = "../create" }
qcow2  = { path = "../qcow2", features = ["create"], default-features = false }
shared = { path = "../../shared" }
vmdk   = { path = "../vmdk", default-features = false }

[lints]
workspace = true
