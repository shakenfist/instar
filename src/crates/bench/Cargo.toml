[package]
name = "bench"
version = "0.2.0"
edition = "2021"
description = "Compute the bench request schedule, transfer split, and flush cadence (no I/O)"
license = "Apache-2.0"
publish = false

[dependencies]
# Phase-6 step 6a (PLAN-qcow2-write-infrastructure-phase-06-bench.md,
# settled decision 1): the refcount-growth planner moved to
# qcow2_write::growth; this dependency backs the temporary re-export
# that keeps src/operations/bench compiling unchanged until 6b.
qcow2-write = { path = "../qcow2-write" }
