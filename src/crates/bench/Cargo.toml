[package]
name = "bench"
version = "0.2.0"
edition = "2021"
description = "Compute the bench request schedule, transfer split, and flush cadence (no I/O)"
license = "Apache-2.0"
publish = false

# This crate is dependency-free: it computes the bench request
# schedule, transfer split, flush cadence and worst-case allocation
# bound (`worst_case_touched`) as pure arithmetic. The refcount-growth
# planner moved to `qcow2_write::growth` in phase-6 step 6a; step 6b
# removed the temporary re-export, so src/operations/bench now imports
# those types from the crate directly.
[dependencies]
