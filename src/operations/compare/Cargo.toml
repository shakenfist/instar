[package]
name = "compare"
version = "0.1.0"
edition = "2021"
description = "Compare operation: image data comparison"
license = "Apache-2.0"

[dependencies]
shared = { path = "../../shared" }
miniz_oxide = { version = "0.8", default-features = false }

[profile.release]
panic = "abort"
opt-level = "z"
lto = true
