[package]
name = "copy"
version = "0.2.0"
edition = "2021"
description = "Copy operation: read from input, write to output"
license = "Apache-2.0"
publish = false

[dependencies]
shared = { path = "../../shared" }

[profile.release]
panic = "abort"
opt-level = "z"
lto = true
