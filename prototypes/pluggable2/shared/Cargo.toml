[package]
name = "shared"
version = "0.1.0"
edition = "2021"
description = "Shared types between core and operations (call table, constants)"
license = "Apache-2.0"

[lib]
# No std, this is used by bare-metal code

[lints.clippy]
unwrap_used = "warn"
